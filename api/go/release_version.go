package main

// releaseVersion is kept in sync with the repository-level VERSION file by the
// security baseline. It is intentionally non-secret build identity metadata.
const releaseVersion = "3.0.2"
